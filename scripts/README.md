# Scripts

This repo is intentionally lightweight. Rather than building script-heavy automation yet, the current MVP uses shell functions and markdown files to capture state and resume work.

Future scripts may be added once the core intake and resume flow is stable.
